#!/bin/sh
# install AUR packages with yaah

cd $HOME/aur && yaah "$@"
for package in "$@"; do
    cd $HOME/aur/"$package" && ${EDITOR:-vi} PKGBUILD

    makepkg -si && cd -

    printf "%s\n" "Build of $package completed."
done

